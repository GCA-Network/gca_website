# Institution logos

The banner on the home page looks for a logo per institution and falls back to
a wordmark when it does not find one. Nothing else needs editing — drop a file
in this folder and it appears on the next page load.

Naming: `<slug>.svg` is tried first, then `<slug>.png`.
SVG is preferred; PNG should be transparent and at least 300px on its long side.
Plates are a fixed 206x66, and logos are capped at 158x38 inside them, so any
aspect ratio is fine.

Row 1 — China and Hong Kong
  tsinghua  peking  hit  fudan  sjtu  tongji  hku  hkust

Row 2 — everywhere else
  harvard  yale  mit  princeton  stanford  cornell  duke  columbia
  berkeley  ucla  ucsd  toronto  ubc  mcgill
  oxford  cambridge  ucl  kcl  eth  tudelft  tum  berlin

Note on rights: university marks are trademarks. Confirm each institution's
brand guidelines permit this use before publishing.
